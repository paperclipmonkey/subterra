{
  "name": "subterra-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright system tests driving the real Laravel + built Vue SPA",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
