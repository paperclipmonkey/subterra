{
    "name": "subterra-image-processor",
    "version": "1.0.0",
    "description": "GCP Cloud Run image processing service for Subterra — resizes uploads to multi-size WebP for srcset",
    "//resolutions": "@opentelemetry/core and uuid are forced above the ranges their parents declare, because upstream has not shipped a patched release within those ranges: @google-cloud/pubsub still pins @opentelemetry/core ^1.30.1 (it uses only W3CTraceContextPropagator, stable across 1.x -> 2.x), and @google-cloud/storage/gaxios/teeny-request still pin uuid ^8/^9 (they call v4() only). Drop these once the parents move.",
    "main": "dist/index.js",
    "scripts": {
        "build": "tsc",
        "typecheck": "tsc --noEmit",
        "start": "node dist/index.js",
        "dev": "ts-node src/index.ts",
        "test": "jest",
        "test:watch": "jest --watch",
        "test:coverage": "jest --coverage"
    },
    "dependencies": {
        "@google-cloud/pubsub": "^5.3.0",
        "@google-cloud/storage": "^7.19.0",
        "@google-cloud/video-transcoder": "^4.4.2",
        "axios": "^1.18.0",
        "express": "^4.22.2",
        "heic-convert": "^2.1.0",
        "sharp": "^0.35.0"
    },
    "devDependencies": {
        "@types/express": "^4.17.21",
        "@types/jest": "^29.5.11",
        "@types/node": "^20.10.6",
        "@types/supertest": "^6.0.3",
        "jest": "^29.7.0",
        "supertest": "^7.2.2",
        "ts-jest": "^29.1.1",
        "ts-node": "^10.9.2",
        "typescript": "^5.3.3"
    },
    "engines": {
        "node": ">=24.0.0"
    },
    "resolutions": {
        "form-data": "^4.0.6",
        "@types/request/form-data": "^2.5.6",
        "protobufjs": "^7.6.3",
        "@opentelemetry/core": "^2.8.0",
        "uuid": "^11.1.1"
    }
}
